.hidden-templates
    #currency-pair-template 
        .currency-pair
            .left
                .inner-frame
                    .title {{{short}}}
                    .score {{{score}}}
            .right
                b.trend-text {{{rightText}}}